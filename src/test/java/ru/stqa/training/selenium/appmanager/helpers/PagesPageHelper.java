package ru.stqa.training.selenium.appmanager.helpers;

import org.openqa.selenium.WebDriver;

public class PagesPageHelper extends HelperBase {
    public PagesPageHelper(WebDriver wd) {
        super(wd);
    }
}
