package ru.stqa.training.selenium.appmanager.helpers;

import org.openqa.selenium.WebDriver;

public class MonthlySalesPageHelper extends HelperBase {
    public MonthlySalesPageHelper(WebDriver wd) {
        super(wd);
    }
}
