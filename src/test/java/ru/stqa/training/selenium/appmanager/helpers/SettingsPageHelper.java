package ru.stqa.training.selenium.appmanager.helpers;

import org.openqa.selenium.WebDriver;

public class SettingsPageHelper extends HelperBase {
    public SettingsPageHelper(WebDriver wd) {
        super(wd);
    }
}
