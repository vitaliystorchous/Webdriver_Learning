package ru.stqa.training.selenium.appmanager.helpers;

import org.openqa.selenium.WebDriver;

public class SecuritySettingsPageHelper extends HelperBase {
    public SecuritySettingsPageHelper(WebDriver wd) {
        super(wd);
    }
}
