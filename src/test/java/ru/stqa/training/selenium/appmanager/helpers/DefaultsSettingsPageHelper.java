package ru.stqa.training.selenium.appmanager.helpers;

import org.openqa.selenium.WebDriver;

public class DefaultsSettingsPageHelper extends HelperBase {
    public DefaultsSettingsPageHelper(WebDriver wd) {
        super(wd);
    }
}
