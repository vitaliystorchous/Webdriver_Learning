package ru.stqa.training.selenium.appmanager.helpers;

import org.openqa.selenium.WebDriver;

public class NewsletterPageHelper extends HelperBase {
    public NewsletterPageHelper(WebDriver wd) {
        super(wd);
    }
}
