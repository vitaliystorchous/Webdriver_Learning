package ru.stqa.training.selenium.appmanager.helpers;

import org.openqa.selenium.WebDriver;

public class VQmodsPageHelper extends HelperBase {
    public VQmodsPageHelper(WebDriver wd) {
        super(wd);
    }
}
