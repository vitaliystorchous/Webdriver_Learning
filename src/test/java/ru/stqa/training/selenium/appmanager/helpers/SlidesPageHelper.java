package ru.stqa.training.selenium.appmanager.helpers;

import org.openqa.selenium.WebDriver;

public class SlidesPageHelper extends HelperBase {
    public SlidesPageHelper(WebDriver wd) {
        super(wd);
    }
}
