package ru.stqa.training.selenium.appmanager.helpers;

import org.openqa.selenium.WebDriver;

public class OrderStatusesPageHelper extends HelperBase {
    public OrderStatusesPageHelper(WebDriver wd) {
        super(wd);
    }
}
